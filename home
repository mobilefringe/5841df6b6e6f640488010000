<div class="flexslider trim_screen">
    <ul class="slides" id="home_banner">            
        <script id="home_banner_template" type="x-tmpl-mustache/text">
            <li>
                <a class="banner_image_li" href="{{url}}" >
                    <img src="{{image_url}}" alt="Banner Image"/>
                </a>
            </li>
        </script>                
    </ul>        
</div>


<script>
    $(document).ready(function(e){
        renderHomeHours();
        function renderAll(){
            var banners = getBanners();
            renderBanner('#home_banner_template','#home_banner', banners);
            var feature_items = getFeatureList();
            console.log(feature_items);
            renderFeatureItems('#feature_item','##feature_item_template', feature_items);
            
            // var stores = getNewStoresList().sortBy(function(o){ return o.created_at });;
            // var new_stores = stores.reverse().slice(0, 3);
            // renderNewStores("#latest_stores", "#latest_stores_template", new_stores);
            
            // var all_events = getEventsList().sortBy(function(o){ return o.show_on_web_date });
            // var event = all_events.reverse().slice(0,1);
            // renderHomeEvents("#latest_events","#latest_events_template", event);
            
            // var all_promotions = getPromotionsList().sortBy(function(o){ return o.show_on_web_date});  
            // var promotions = all_promotions.slice(0,2);
            // renderPromotions("#latest_promos","#latest_promos_template", promotions);

            // $('.flexslider').flexslider({
            //     animation: "slide",
            // });
        }
        loadMallData(renderAll); 
    })
</script>