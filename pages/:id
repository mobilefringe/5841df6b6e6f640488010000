<style>
    #error_msg {
        text-align: center;
    }
</style>
<div class="page_body">
    <h1 class="page_title" id="page_title"></h1>
    <hr>
    <div id="contact_us" class="hidden">
        <iframe class="hidden-phone" width="750" height="450" frameborder="0" scrolling="no" marginheight="0" marginwidth="0" src="{{google_map_url}}&amp;iwloc=A&amp;output=embed"></iframe>
        <br/>
        <!--<a class="visible-phone" href="http://maps.google.ca?q={{site.property.name}}" target="_blank">-->
        <!--    <img src="http://maps.googleapis.com/maps/api/staticmap?center={{site.property.latitude}},{{site.property.longitude}}&amp;zoom=16&amp;size=600x500&amp;maptype=roadmap&amp;sensor=false&amp;markers=color:red%7Ccolor:red%7Clabel:%7C{{site.property.latitude}},{{site.property.longitude}}">    -->
        <!--</a>-->
        <div sylte="clear:both;">&nbsp;</div>    
    </div>
    <div id="page_body"></div>
</div>
<script>
    $(document).ready(function(e){
        init(e);
        function renderAll(){
            var pathArray = window.location.pathname.split( '/' );
            var slug = pathArray[pathArray.length-1];
            var pages_json = "//www.aberdeenmall.ca/pages/" + slug + ".json"
            $.getJSON(pages_json).done(function(data) {
                $('#page_title').html(data.title);
                $('#page_body').html(data.body);
                if(slug === "aberdeen-contact-us") {
                    $("#contact_us").show();
                }
            }).fail(function(jqXHR) {
                if (jqXHR.status == 404) {
                    $("#error_msg").fadeIn("fast");
                }
            }); 
        }
        loadMallDataCached(renderAll); 
    })
</script>